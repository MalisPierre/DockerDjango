version: '3.9'

services:

  backend:
    container_name: backend
    build:
      context: ./backend
    ports:
      - 8000:8000
    volumes:
      - ./backend:/backend
      - ./frontend:/frontend
      - ./data/web:/vol/web
